name: CI
on:
  push:
    branches:
      - main
    tags: ['*']
  pull_request:
  workflow_dispatch:
concurrency:
  # Skip intermediate builds: always.
  # Cancel intermediate builds: only if it is a pull request build.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}
jobs:
  test:
    name: ${{ matrix.package }} - Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.experimental == true }}
    timeout-minutes: 60
    permissions: # needed to allow julia-actions/cache to proactively delete old caches that it has created
      actions: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        # The two packages live in sibling subdirectories of this monorepo.
        # `setup-julia`'s `project` input resolves `min` from each package's
        # own [compat], so they can carry different lower bounds.
        package:
          - JuliaLibWrapping
          - JLWInterop
        version:
          - 'min'
          - 'pre'
          - 'nightly'
        os:
          - ubuntu-latest
        arch:
          - x64
        include:
          - package: JuliaLibWrapping
            coverage_dirs: JuliaLibWrapping/src,JuliaLibWrapping/ext
          - package: JLWInterop
            coverage_dirs: JLWInterop/src
          # JuliaLibWrapping's compat floor (1.13) has no stable release yet, so
          # setup-julia cannot resolve `min` today. Keep the cell so it starts
          # testing 1.13.0 automatically once it ships; soft-fail until then.
          - package: JuliaLibWrapping
            version: 'min'
            experimental: true
          # juliac --trim is currently broken on Julia nightly (verifier
          # errors in Base, e.g. unresolved HashState/IOContext calls);
          # keep the signal visible without failing the workflow.
          - package: JuliaLibWrapping
            version: 'nightly'
            experimental: true
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: ${{ matrix.version }}
          arch: ${{ matrix.arch }}
          project: ${{ matrix.package }}
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-buildpkg@v1
        with:
          project: ${{ matrix.package }}
      # The `ols` and `boundary` example smoke tests import numpy.
      - uses: actions/setup-python@v7
        if: matrix.package == 'JuliaLibWrapping'
        with:
          python-version: '3.x'
      - name: Install numpy
        if: matrix.package == 'JuliaLibWrapping'
        run: python3 -m pip install numpy
      - uses: julia-actions/julia-runtest@v1
        with:
          project: ${{ matrix.package }}
      - uses: julia-actions/julia-processcoverage@v1
        with:
          directories: ${{ matrix.coverage_dirs }}
      - uses: codecov/codecov-action@v7
        with:
          files: lcov.info
          flags: ${{ matrix.package }}
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
  matlab:
    name: MATLAB gate
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      actions: write
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: 'pre'
          project: JuliaLibWrapping
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-buildpkg@v1
        with:
          project: JuliaLibWrapping
      # Licensed automatically on GitHub-hosted runners for public
      # repositories, so this needs no secret.
      - uses: matlab-actions/setup-matlab@v2
        with:
          release: R2021a
      - name: Instantiate the example's build environment
        run: |
          julia --project=JuliaLibWrapping/examples/boundary/build-env \
                -e 'using Pkg; Pkg.instantiate()'
      - name: Build the boundary example
        working-directory: JuliaLibWrapping/examples/boundary
        run: julia build.jl
      # `build_mex` compiles the gateway; `smoke` calls every wrapped
      # function and throws on a mismatch, so -batch exits non-zero.
      - uses: matlab-actions/run-command@v2
        with:
          command: |
            cd JuliaLibWrapping/examples/boundary/out
            build_mex
            cd ../test
            smoke

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    permissions:
      actions: write # needed to allow julia-actions/cache to proactively delete old caches that it has created
      contents: write
      statuses: write
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: 'pre'
          project: JuliaLibWrapping/docs
      - uses: julia-actions/cache@v3
      - name: Build and deploy documentation
        # The docs environment pulls JuliaLibWrapping and JLWInterop in via its
        # own [sources], so a plain instantiate is enough — no separate
        # Pkg.develop step. make.jl is run directly as a script (not via
        # `include` under a `julia {0}` shell, where a relative include path
        # resolves against the temp script's directory rather than the repo
        # root); makedocs derives its root from make.jl's own location.
        # julia-docdeploy is avoided because it hard-codes a top-level `docs/`.
        run: |
          julia --project=JuliaLibWrapping/docs -e 'using Pkg; Pkg.instantiate()'
          julia --project=JuliaLibWrapping/docs --color=yes JuliaLibWrapping/docs/make.jl
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DOCUMENTER_KEY: ${{ secrets.DOCUMENTER_KEY }}
